# dot-fit 개발 지침

## 기본 환경

- React Native + Expo SDK 57 기준으로 작업한다.
- Expo Router와 Development Build 환경을 기준으로 검토한다.
- TypeScript를 추가하지 않고 JavaScript만 사용한다.
- Android를 우선 지원한다.
- 현재 프로젝트 구조와 기존 코드를 먼저 확인한 뒤 수정한다.
- 요청받지 않은 기능이나 라이브러리를 임의로 추가하지 않는다.

## 코드 및 데이터 관리

- 화면 컴포넌트와 Health Connect, 저장소 등의 비즈니스 로직을 분리한다.
- 반복되는 문자열, 저장소 키, 설정값은 상수로 관리한다.
- 기존 사용자 데이터, 캐릭터 정보, 코인, 목표 걸음 수, 온보딩 완료 상태를 초기화하지 않는다.
- 기존 저장 데이터와의 하위 호환성을 고려한다.
- 현재 핵심 활동 지표는 걸음 수이며, 거리와 소모 열량은 별도 요청 없이 추가하지 않는다.

## Health Connect 걸음 수 정책

- 여러 데이터 소스 중 가장 큰 걸음 수를 선택하지 않는다.
- 현재 기기 자체에서 측정한 걸음 수 조회에 성공하면 해당 값을 사용한다.
- 기기 걸음 수를 식별하거나 조회하지 못한 경우에만 전체 집계값을 fallback으로 사용한다.
- 두 조회가 모두 실패한 경우 0을 반환한다.
- `Math.max(deviceSteps, aggregateSteps)` 방식을 사용하지 않는다.
- 기기 걸음 수와 Samsung Health 등의 외부 데이터를 단순 합산하지 않는다.
- DataOrigin의 특정 패키지명을 영구적으로 고정하지 않는다.
- Health Connect 오류는 추측으로 수정하지 않고 실제 로그, 반환값, 권한 상태 및 설치 버전을 먼저 확인한다.

## 캐릭터 기본 정책

- 캐릭터는 동물이 아닌 사람형 도트 캐릭터로 제작한다.
- 남성형과 여성형을 구분하여 제공한다.
- 남성형과 여성형 모두 동일한 활동 상태와 판정 기준을 적용한다.
- 누티 타입: 남성형 외모의 기본 캐릭터
- 루티 타입: 여성형 외모의 기본 캐릭터
- 상태가 변경되어도 얼굴, 머리, 대표 색상 등 캐릭터의 고유 특징은 유지한다.
- 체형뿐 아니라 표정, 자세, 동작 및 주변 효과를 함께 사용해 활동 상태를 표현한다.

## 캐릭터 활동 상태

캐릭터 활동 상태는 평균 걸음 수에 따라 다음 5단계로 구분한다.

|  평균 걸음 수 | 상태          | 디자인 방향                                   |
| ------------: | ------------- | --------------------------------------------- |
|     0~2,999보 | 나른한        | 배와 볼이 통통하고 음식을 들고 웃고 있는 모습 |
| 3,000~4,999보 | 안정적인      | 약간 통통하고 편안한 모습                     |
| 5,000~6,999보 | 활기찬        | 균형 잡힌 일반 체형과 밝은 표정               |
| 7,000~8,999보 | 에너지 넘치는 | 군살이 줄고 탄탄하며 역동적인 모습            |
|  9,000보 이상 | 왕성한        | 군살 없는 체형, 또렷한 눈빛과 주변 아우라     |

영문 상태명은 다음과 같이 사용한다.

`Drowsy → Steady → Spry → Energetic → Vigorous`

## 평균 걸음 수 계산

- 기기에 30일 이상의 데이터가 있으면 최근 30일의 평균 걸음 수를 사용한다.
- 데이터가 30일 미만이면 기기에서 조회 가능한 전체 일수의 평균을 사용한다.
- 별도의 최소 데이터 기간, 측정 중 상태, 연속 유지 조건 또는 복잡한 가중치를 적용하지 않는다.
- 조회 가능한 걸음 수 데이터가 전혀 없으면 기본 상태인 `활기찬`을 적용한다.

## UI 및 접근성

- 밝고 친근한 건강관리 앱의 기존 디자인을 유지한다.
- 캐릭터가 홈 화면의 시각적 중심이 되도록 한다.
- Safe Area와 다양한 모바일 화면 크기를 고려한다.
- 버튼의 터치 영역, 접근성 라벨, 로딩 상태 및 오류 상태를 함께 검토한다.
- 요청받지 않은 UI 영역이나 기존 색상 규칙을 임의로 변경하지 않는다.

## 검증

작업 후 가능한 범위에서 다음 검증을 수행한다.

- 변경 파일 ESLint 검사
- Android 프로덕션 번들 생성
- `git diff --check`
- 기존 사용자 데이터 호환성 확인
- Health Connect fallback 정책 확인
- 네이티브 설정 변경 여부 확인

JavaScript와 스타일만 변경했다면 Development Build 재빌드가 필요하지 않다고 보고한다.

네이티브 모듈, AndroidManifest, Gradle, Expo config plugin 또는 네이티브 권한 설정을 변경했다면 Development Build 재빌드가 필요하다고 보고한다.

## 작업 결과 보고

작업 완료 후 다음 내용을 간결하게 보고한다.

1. 변경 및 추가한 파일
2. 구현된 최종 동작
3. 기존 데이터 및 기능에 미치는 영향
4. 검증 결과
5. Development Build 재빌드 필요 여부
6. 실제 기기에서 추가 확인이 필요한 항목
